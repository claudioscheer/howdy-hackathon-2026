# Copy to .env.local. Do not commit real secrets.
# The app does not read these yet. Compose uses them for Postgres.

POSTGRES_USER=howdy
POSTGRES_PASSWORD=change-me
POSTGRES_DB=interview_coach
DATABASE_URL=postgresql://howdy:change-me@localhost:5432/interview_coach
